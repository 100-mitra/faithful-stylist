"""FastAPI application layer: routes, schemas, and the minimal demo UI.

Depends on ``core`` (which never imports FastAPI). Endpoints are fleshed out in
Phase 1 step 11; Phase 0 ships only ``/healthz/``.
"""
