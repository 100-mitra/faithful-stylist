"""Grounded rationale generation: the LLM picks WHICH facts to surface and writes
opinion clauses; code renders the factual text from the record; the GroundingVerifier
audits every claim. Output is tagged factual/subjective with a grounded flag.

Implemented in Phase 1 step 9.
"""
